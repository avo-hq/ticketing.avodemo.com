<div class="flex flex-col grow py-2 md:items-center md:flex-row border-b">
  <div class="px-4 w-64 font-semibold">
    <p class="text-gray-700 font-semibold"><%= comment.user.name %></p>
    <p class="pt-1 text-xs font-medium">[<%= comment.created_at %>]</p>
  </div>
  <p class="flex-1 px-6"><%= comment.content %></p>
</div>
